# Python
__pycache__/
*.pyc
.venv/
venv/
.env
*.egg-info/

# AWS / credentials
.aws/
*.pem

# Editor / OS
.DS_Store
.vscode/

# Claude Code / MCP local config (session-local, not project content)
.claude/
.mcp.json

# Data (raw report downloads — don't commit large PDFs/HTML to git)
data/raw/
